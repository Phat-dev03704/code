═══════════════════════════════════════════════════════════════════════════════
   HƯỚNG DẪN SỬ DỤNG - MACHINE LEARNING / DEEP LEARNING APPROACHES FOR VRPTW
═══════════════════════════════════════════════════════════════════════════════

Thư mục này chứa các giải pháp VRPTW dựa trên Machine Learning và Deep Learning.
Các models cần được TRAIN trước khi sử dụng để giải VRPTW.

═══════════════════════════════════════════════════════════════════════════════
[1] ATTENTION MODEL (Transformer-based Reinforcement Learning)
═══════════════════════════════════════════════════════════════════════════════

📂 Vị trí: NeuralCombinatorialOptimization/Attention-Model/

Mô tả: Sử dụng Attention Mechanism (Transformer) kết hợp với RL để học cách giải VRPTW

BƯỚC 1: Training Model
-----------------------------------------------------------
cd NeuralCombinatorialOptimization/Attention-Model
python train.py

Tham số training:
  --n-epochs N         Số epochs (mặc định: 100)
  --batch-size N       Batch size (mặc định: 64)
  --lr RATE           Learning rate (mặc định: 0.001)
  --embedding-dim N    Embedding dimension (mặc định: 128)

BƯỚC 2: Test với Trained Model
-----------------------------------------------------------
# Test một dataset
python attention_vrptw_solver.py

# Test tất cả datasets
python test_all_datasets.py

Kết quả: result/images/, result/text/, result/test_summary.csv
Model: models/attention_model_best.pth

═══════════════════════════════════════════════════════════════════════════════
[2] DQN-RL (Deep Q-Network Reinforcement Learning)
═══════════════════════════════════════════════════════════════════════════════

📂 Vị trí: NeuralCombinatorialOptimization/DQN-RL/

Mô tả: Sử dụng Deep Q-Network để học policy giải VRPTW thông qua trial-and-error

BƯỚC 1: Training Model
-----------------------------------------------------------
cd NeuralCombinatorialOptimization/DQN-RL
python train.py

Tham số training:
  --n-episodes N       Số episodes (mặc định: 1000)
  --batch-size N       Batch size (mặc định: 64)
  --lr RATE           Learning rate (mặc định: 0.001)
  --gamma RATE        Discount factor (mặc định: 0.99)

BƯỚC 2: Test với Trained Model
-----------------------------------------------------------
# Test một dataset
python dqn_vrptw_solver.py

# Test tất cả datasets
python test_all_datasets.py

Kết quả: result/training_curves.png
Model: models/dqn_model_best.pth

📖 Xem thêm: TRAINING_GUIDE.md

═══════════════════════════════════════════════════════════════════════════════
[3] POINTER NETWORK (Sequence-to-Sequence with Attention)
═══════════════════════════════════════════════════════════════════════════════

📂 Vị trí: NeuralCombinatorialOptimization/Pointer-Network/

Mô tả: Sử dụng Pointer Network (Seq2Seq + Attention) để output sequence các điểm

BƯỚC 1: Training Model
-----------------------------------------------------------
cd NeuralCombinatorialOptimization/Pointer-Network
python train.py

Tham số training:
  --n-epochs N         Số epochs (mặc định: 100)
  --batch-size N       Batch size (mặc định: 128)
  --lr RATE           Learning rate (mặc định: 0.001)
  --hidden-dim N      Hidden dimension (mặc định: 128)

BƯỚC 2: Test với Trained Model
-----------------------------------------------------------
# Test một dataset
python pointer_network_vrptw_solver.py

# Test tất cả datasets
python test_all_datasets.py

Kết quả: result/images/, result/text/, result/test_summary.csv
Model: models/pointer_network_best.pth

📖 Xem thêm: TRAINING_GUIDE.md

═══════════════════════════════════════════════════════════════════════════════
[4] SO SÁNH CÁC PHƯƠNG PHÁP ML/DL
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────┬──────────────────┬────────────────┬─────────────────────┐
│ Method          │ Training Time    │ Inference Time │ Solution Quality    │
├─────────────────┼──────────────────┼────────────────┼─────────────────────┤
│ Attention Model │ 2-4 hours        │ < 1s           │ Tốt                 │
│ DQN-RL          │ 4-8 hours        │ < 1s           │ Trung bình          │
│ Pointer Network │ 3-6 hours        │ < 1s           │ Tốt                 │
└─────────────────┴──────────────────┴────────────────┴─────────────────────┘

ƯU ĐIỂM:
  ✓ Inference rất nhanh (< 1 giây)
  ✓ Có thể generalize cho các instances mới
  ✓ End-to-end learning, không cần thiết kế heuristic
  ✓ Có thể cải thiện qua training thêm

NHƯỢC ĐIỂM:
  ✗ Cần thời gian training dài
  ✗ Cần GPU để training hiệu quả
  ✗ Chất lượng solution phụ thuộc vào training data
  ✗ Khó giải thích (black-box)

═══════════════════════════════════════════════════════════════════════════════
[5] YÊU CẦU HỆ THỐNG
═══════════════════════════════════════════════════════════════════════════════

Python Packages:
  - torch (PyTorch)
  - numpy, pandas
  - matplotlib
  
Hardware (khuyến nghị):
  - GPU (CUDA) cho training
  - CPU đủ cho inference
  - RAM: 8GB+

═══════════════════════════════════════════════════════════════════════════════
LƯU Ý: Các ML/DL models cần được TRAIN trước khi sử dụng. Thời gian training có
       thể mất vài giờ. Nếu chưa có model trained, hãy chạy train.py trước.
═══════════════════════════════════════════════════════════════════════════════
